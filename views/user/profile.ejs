<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/owl.carousel.min.css">
    <link rel="stylesheet" href="/assets/css/flaticon.css">
    <link rel="stylesheet" href="/assets/css/slicknav.css">
    <link rel="stylesheet" href="/assets/css/animate.min.css">
    <link rel="stylesheet" href="/assets/css/magnific-popup.css">
    <link rel="stylesheet" href="/assets/css/fontawesome-all.min.css">
    <link rel="stylesheet" href="/assets/css/themify-icons.css">
    <link rel="stylesheet" href="/assets/css/slick.css">
    <link rel="stylesheet" href="/assets/css/nice-select.css">
    <link rel="stylesheet" href="/assets/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">

    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css" rel="stylesheet">


    <style>
        .modal-backdrop {
            z-index: 1040;
        }

        .modal {
            z-index: 1050;
        }

        .reason-textarea {
            resize: none;
        }

        .error-feedback {
            color: #dc3545;
            font-size: 0.875rem;
            margin-top: 0.25rem;
            display: none;
        }

        .sidebar {
            background: #f8f9fa;
            min-height: calc(100vh - 100px);
            border-right: 1px solid #dee2e6;
        }

        .sidebar .nav-link {
            color: #333;
            padding: 0.8rem 1rem;
            border-radius: 0.25rem;
            margin: 0.2rem 0;
            cursor: pointer;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            background: #e9ecef;
            color: #ff2020;
        }

        .profile-section {
            padding: 2rem;
        }

        .section {
            display: none;
        }

        .section.active {
            display: block;
        }

        .address-card {
            border: 1px solid #dee2e6;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1rem;
        }

        .order-card {
            border: 1px solid #dee2e6;
            border-radius: 0.5rem;
            margin-bottom: 1rem;
        }

        .text-danger {
            color: #e74c3c;
            font-size: 0.875rem;
        }







        .wallet-card {
            background: linear-gradient(135deg, #5a6e83 0%, #3498db 100%);
            color: white;
            border-radius: 10px;
            padding: 15px;
            box-shadow: 0 4px 6px rgba(188, 171, 171, 0.2);
            max-width: 777px;
            max-height: 225px;
            margin: 0 auto;
        }

        .wallet-number {
            letter-spacing: 3px;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            color: rgb(4, 4, 4);
        }

        .transaction-table {
            font-size: 0.85rem;
        }

        .transaction-table {
            background-color: white;
        }

        .transaction-table thead {
            background-color: #f8f9fa;
        }


        .custom-pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }

        .pagination-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #f8f9fa;
            color: #495057;
            border: none;
            transition: all 0.3s ease;
        }

        .pagination-btn:hover {
            background-color: #dc3545;
            color: white;
        }

        .pagination-btn.active {
            background-color: #dc3545;
            color: white;
        }

        .pagination-btn.disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }



        /* .modal-overlay {
            background-color: rgba(0,0,0,0.5);
        }
        #otherReasonContainer, 
        #otherReturnReasonContainer {
            transition: all 0.3s ease;
        } */


        .card {
            border: none;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .stats-container {
            padding: 10px;
        }

        .input-group .form-control {
            border-radius: 6px;
            padding: 0.75rem 1rem;
            background-color: #f8f9fa;
        }

        .btn {
            font-weight: 500;
            height: 35px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .btn i {
            font-size: 0.9rem;
        }
    </style>

</head>

<body>
    <%- include('../partials/user/header') %>

        <section class="profile-container py-5">
            <div class="container-fluid">
                <div class="row">
                    <!-- Sidebar -->
                    <div class="col-lg-3 col-md-4 sidebar py-4">
                        <div class="text-center mb-4">
                            <i class="fa-regular fa-user"></i>

                            <h5>
                                <%= userData.name %>
                            </h5>
                            <p class="text-muted">
                                <%= userData.email %>
                            </p>
                        </div>
                        <nav class="nav flex-column">
                            <a class="nav-link active" data-section="profile">
                                <i class="fas fa-user"></i> My Profile
                            </a>
                            <a class="nav-link" data-section="orders">
                                <i class="fas fa-shopping-bag"></i> My Orders
                            </a>
                            <a class="nav-link" data-section="addresses">
                                <i class="fas fa-map-marker-alt"></i> My Addresses
                            </a>
                            <a class="nav-link" data-section="password">
                                <i class="fas fa-lock"></i> Change Password
                            </a>
                            <a class="nav-link" data-section="wallet">
                                <i class="fa-solid fa-wallet"></i> Wallet
                            </a>
                            <a class="nav-link" data-section="referals">
                                <i class="fa-solid fa-gift"></i> Referals
                            </a>
                        </nav>
                    </div>

                    <!-- Main Content -->
                    <div class="col-lg-9 col-md-8 profile-section">
                        <!-- Profile Section -->
                        <div id="profile" class="section active">
                            <h3>My Profile</h3>
                            <div class="card">
                                <div class="card-body">
                                    <form id="profileForm">
                                        <div class="row mb-3">
                                            <div class="col-md-12">
                                                <label class="form-label">First Name</label>
                                                <input type="text" id="firstName" class="form-control"
                                                    value="<%= userData.name %>">
                                                <span id="firstNameError" class="text-danger small"></span>
                                            </div>
                                        </div>
                                        <div class="row mb-3">
                                            <div class="col-md-6">
                                                <label class="form-label">Email</label>
                                                <input type="email" id="email" class="form-control"
                                                    value="<%= userData.email %>" disabled>
                                                <span id="emailError" class="text-danger small"></span>
                                            </div>
                                            <div class="col-md-6">
                                                <label class="form-label">Phone</label>
                                                <input type="tel" id="phone" class="form-control"
                                                    value="<%= userData.phone %>">
                                                <span id="phoneError" class="text-danger small"></span>
                                            </div>
                                        </div>
                                        <button type="submit" class="btn btn-primary">Update Profile</button>
                                    </form>
                                </div>
                            </div>
                        </div>


                        <!-- Order Summary Card -->
                        <div id="orders" class="section">
                            <h3>My Orders</h3>
                            <% if (order && order.length> 0) { %>
                                <% order.forEach(function(orderItem) { %>
                                    <div class="card mb-3">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-center">
                                                <div>
                                                    <h6 class="mb-1">Order ID: <%= orderItem.orderedId %>
                                                    </h6>
                                                    <p class="text-muted mb-0 small">
                                                        <%= new Date(orderItem.createdAt).toLocaleDateString() %>
                                                    </p>
                                                    <span class="badge bg-primary">
                                                        <%= orderItem.orderStatus %>
                                                    </span>
                                                </div>
                                                <button class="btn btn-primary btn-sm" data-bs-toggle="modal"
                                                    data-bs-target="#orderModal<%= orderItem.orderedId %>">
                                                    View Order
                                                </button>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Order Details Modal -->
                                    <div class="modal fade" id="orderModal<%= orderItem.orderedId %>" tabindex="-1"
                                        aria-hidden="true">
                                        <div class="modal-dialog modal-lg">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title">Order Details ID: <%= orderItem.orderedId %>
                                                    </h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                        aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body">
                                                    <!-- Order Items -->
                                                    <% orderItem.items.forEach(function(item) { %>
                                                        <div class="row mb-3 align-items-center">
                                                            <div class="col-md-2">
                                                                <img src="<%= item.image %>" alt="Product Image"
                                                                    class="img-fluid rounded">
                                                            </div>
                                                            <div class="col-md-6">
                                                                <h6 class="mb-1">
                                                                    <%= item.productId.name %>
                                                                </h6>
                                                                <p class="text-muted mb-1">Quantity: <%= item.quantity
                                                                        %>
                                                                </p>
                                                                <% if (item.isCanceled) { %>
                                                                    <span class="badge bg-danger">Cancelled</span>
                                                                    <% if (item.canceledItems) { %>
                                                                        <span class="text-muted">(<%= item.canceledItems
                                                                                %> items)</span>
                                                                        <% } %>
                                                                            <% } %>
                                                            </div>
                                                            <div class="col-md-4 text-end">
                                                                <h6 class="mb-1">₹<%= item.price.toFixed(2) %>
                                                                </h6>
                                                                <small class="text-muted">
                                                                    <%= orderItem.paymentMethod.toUpperCase() %>
                                                                </small>
                                                            </div>
                                                        </div>
                                                        <% }); %>

                                                            <!-- Delivery Address -->
                                                            <div class="border-top pt-3 mt-3">
                                                                <div class="row">
                                                                    <div class="col-md-6">
                                                                        <h6>Delivery Address:</h6>
                                                                        <p class="mb-1">
                                                                            <%= orderItem.address.name %>
                                                                        </p>
                                                                        <p class="mb-1">
                                                                            <%= orderItem.address.address %>
                                                                        </p>
                                                                        <p class="mb-1">
                                                                            <%= orderItem.address.locality %>,
                                                                                <%= orderItem.address.place %>,
                                                                                    <%= orderItem.address.state %> -
                                                                                        <%= orderItem.address.pincode %>
                                                                        </p>
                                                                        <p class="mb-1">Phone: <%=
                                                                                orderItem.address.phone %>
                                                                        </p>
                                                                    </div>
                                                                    <div class="col-md-6 text-end">
                                                                        <div class="mb-2">
                                                                            <span class="text-muted">Items Total:</span>
                                                                            <span class="ms-2">₹<%=
                                                                                    orderItem.totalAmount.toFixed(2) %>
                                                                            </span>
                                                                        </div>
                                                                        <div class="fw-bold">
                                                                            <span>Total Payable:</span>
                                                                            <span class="ms-2">₹<%=
                                                                                    orderItem.payableAmount.toFixed(2)
                                                                                    %></span>
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                            </div>
                                                </div>
                                                <div class="modal-footer">
                                                    <% if (!['Canceled' , 'Returned' ,'Delivered','Pending'
                                                        ].includes(orderItem.orderStatus)) { %>
                                                        <button class="btn btn-outline-danger"
                                                            onclick="cancelOrder('<%= orderItem._id %>')">
                                                            Cancel Order
                                                        </button>
                                                        <% } %>
                                                            <% if (['Failed'].includes(orderItem.paymentStatus)) { %>
                                                                <button class="btn btn-primary"
                                                                    onclick="retrypayment('<%= orderItem._id %>')">
                                                                    Retry Payment
                                                                </button>
                                                                <% } %>

                                                                    <% if
                                                                        (!['Failed'].includes(orderItem.paymentStatus))
                                                                        { %>
                                                                        <button id="downloadPDF" class="btn btn-danger"
                                                                            onclick="downloadPDF('<%= orderItem._id %>')">Download
                                                                            PDF</button>
                                                                        <% } %>
                                                                            <% if (['Delivered'
                                                                                ].includes(orderItem.orderStatus)) { %>
                                                                                <button class="btn btn-outline-danger"
                                                                                    onclick="returnOrder('<%= orderItem._id %>')">
                                                                                    Return Order
                                                                                </button>
                                                                                <% } %>
                                                                                    <button type="button"
                                                                                        class="btn btn-secondary"
                                                                                        data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <% }); %>
                                        <% } else { %>
                                            <div class="empty-orders text-center py-5">
                                                <i class="fas fa-shopping-bag mb-3"
                                                    style="font-size: 3rem; color: #ccc;"></i>
                                                <h5>No Orders Yet</h5>
                                                <p class="text-muted">Start shopping to see your orders here!</p>
                                                <a href="/shop" class="btn btn-primary mt-3">Start Shopping</a>
                                            </div>
                                            <% } %>
                        </div>



                        <!-- Addresses Section -->
                        <div id="addresses" class="section">
                            <h3>My Addresses</h3>
                            <a href="/add-address" class="btn btn-primary mb-3">
                                <i class="fas fa-plus"></i> Add New Address
                            </a>
                            <div class="tab-content" id="nav-tabContent">
                                <div class="tab-pane fade show active" id="nav-home" role="tabpanel"
                                    aria-labelledby="nav-home-tab">
                                    <div class="row">
                                        <% if (addresses.length===0) { %>
                                            <div class="col-12 text-center">
                                                <p>No addresses available at the moment.</p>
                                            </div>
                                            <% } else { %>
                                                <% addresses.forEach(address=> { %>
                                                    <div class="col-md-6">
                                                        <div class="address-card">
                                                            <div class="d-flex justify-content-between mb-2">
                                                                <div>
                                                                    <p>
                                                                        <%= address.addressType %>
                                                                    </p>
                                                                </div>
                                                                <div>
                                                                    <h6>
                                                                        <% if (address.isPrimary===true) { %>
                                                                            <p><b>Primary Address:</b> </p>
                                                                            <% } %>
                                                                    </h6>
                                                                </div>
                                                                <div>
                                                                    <a href="/edit-address/<%= address._id %>"
                                                                        class="text-primary me-2"><i
                                                                            class="fas fa-edit"></i></a>
                                                                    <a href="#" class="text-danger delete-btn"
                                                                        data-id="<%= address._id %>">
                                                                        <i class="fas fa-trash"></i>
                                                                    </a>
                                                                </div>
                                                            </div>
                                                            <p class="mb-1">
                                                                <%= address.name %>
                                                            </p>
                                                            <p class="mb-1">
                                                                <%= address.locality %>
                                                            </p>
                                                            <p class="mb-1">
                                                                <%= address.address %>
                                                            </p>
                                                            <p class="mb-1">
                                                                <%= address.place %>, <%= address.state %>
                                                                        <%= address.pincode %>
                                                            </p>
                                                            <p class="mb-1">Phone: <%= address.phone %>
                                                            </p>
                                                            <% if (address.alternatePhone) { %>
                                                                <p class="mb-1">Alternate Phone: <%=
                                                                        address.alternatePhone %>
                                                                </p>
                                                                <% } %>
                                                                    <% if (address.landmark) { %>
                                                                        <p class="mb-1">Landmark: <%= address.landmark
                                                                                %>
                                                                        </p>
                                                                        <% } %>


                                                        </div>
                                                    </div>
                                                    <% }) %>
                                                        <% } %>
                                    </div>
                                </div>
                            </div>

                        </div>

                        <!-- Change Password Section -->
                        <div id="password" class="section">
                            <h3>Change Password</h3>
                            <div class="card">
                                <div class="card-body">
                                    <form id="passwordForm">
                                        <div class="mb-3">
                                            <label for="currentPassword" class="form-label">Current Password</label>
                                            <input type="password" id="currentPassword" class="form-control">
                                            <div id="currentPasswordError" class="text-danger"></div>
                                        </div>
                                        <div class="mb-3">
                                            <label for="newPassword" class="form-label">New Password</label>
                                            <input type="password" id="newPassword" class="form-control">
                                            <div id="newPasswordError" class="text-danger"></div>
                                        </div>
                                        <div class="mb-3">
                                            <label for="confirmNewPassword" class="form-label">Confirm New
                                                Password</label>
                                            <input type="password" id="confirmNewPassword" class="form-control">
                                            <div id="confirmNewPasswordError" class="text-danger"></div>
                                        </div>
                                        <button type="submit" class="btn btn-primary">Update Password</button>
                                    </form>
                                </div>
                            </div>
                        </div>



                        <!-- <div id="wallet" class="section">
                            <h3>Wallet</h3>
                            <div class="card">
                                <div class="card-body">
                                    
                                </div>
                            </div>
                        </div> -->



                        <!-- wallet.ejs -->
                        <div id="wallet" class="section">
                            <h3 class="mb-4">Wallet</h3>
                            <div class="card wallet-card">
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-md-6">
                                            <h4 class="mb-3">Your Wallet Balance</h4>
                                            <h2 class="display-4 mb-3">₹ <%= walletBalance.toFixed(2) %>
                                            </h2>
                                            <p class="wallet-number">
                                                Virtual Wallet **** **** 1234
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="card mt-4">
                                <div class="card-header">
                                    <h5 class="card-title">Transaction History</h5>
                                </div>
                                <div class="card-body">
                                    <div class="table-responsive">
                                        <table class="table transaction-table table-hover">
                                            <thead>
                                                <tr>
                                                    <th>#</th>
                                                    <th>Amount</th>
                                                    <th>Type</th>
                                                    <th>Description</th>
                                                    <th>Date</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% if (transactions.length===0) { %>
                                                    <tr>
                                                        <td colspan="5" class="text-center">No transactions available
                                                        </td>
                                                    </tr>
                                                    <% } else { %>
                                                        <% transactions.forEach((transaction, index)=> { %>
                                                            <tr>
                                                                <td>
                                                                    <%= index + 1 %>
                                                                </td>
                                                                <td>₹ <%= transaction.amount.toFixed(2) %>
                                                                </td>
                                                                <td>
                                                                    <span
                                                                        class="badge <%= transaction.type === 'Credit' ? 'bg-success' : 'bg-danger' %>">
                                                                        <%= transaction.type %>
                                                                    </span>
                                                                </td>
                                                                <td>
                                                                    <%= transaction.description %>
                                                                </td>
                                                                <td>
                                                                    <%= new Date(transaction.createdAt).toLocaleString()
                                                                        %>
                                                                </td>
                                                            </tr>
                                                            <% }) %>
                                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>

                                    <nav aria-label="Transaction history pagination" class="mt-3">
                                        <div class="custom-pagination">
                                            <button class="pagination-btn disabled" aria-label="Previous">
                                                <i class="bi bi-chevron-left"></i>
                                            </button>
                                            <button class="pagination-btn active">1</button>
                                            <button class="pagination-btn">2</button>
                                            <button class="pagination-btn">3</button>
                                            <button class="pagination-btn" aria-label="Next">
                                                <i class="bi bi-chevron-right"></i>
                                            </button>
                                        </div>
                                    </nav>
                                </div>
                            </div>
                        </div>



                        <div id="referals" class="section">
                            <h3>Your Referral Dashboard</h3>

                            <!-- Stats Card -->
                            <div class="card mb-4">
                                <div class="card-body">
                                    <div class="row g-4">
                                        <div class="col-md-6">
                                            <div class="stats-container">
                                                <div class="d-flex align-items-center mb-2">
                                                    <i class="fa-solid fa-users text-primary fs-4 me-2"></i>
                                                    <h6 class="mb-0 text-muted">Total Referrals</h6>
                                                </div>

                                                <%= console.log(user.referralCount ) %>
                                                    <h4 class="mb-0 fw-bold ps-4">
                                                        <%= user.referralCount %>
                                                    </h4>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="stats-container">
                                                <div class="d-flex align-items-center mb-2">
                                                    <i class="fa-solid fa-gift text-success fs-4 me-2"></i>
                                                    <h6 class="mb-0 text-muted">Rewards Earned</h6>
                                                </div>
                                                <h4 class="mb-0 fw-bold ps-4">$<%= (user.referralCount || 0) * 100 %>
                                                </h4>
                                            </div>
                                        </div>
                                    </div>
                                </div>


                                <!-- Referral Link Card -->
                                <div class="card">
                                    <div class="card-body">
                                        <h5 class="card-title mb-4">Share Your Referral Link</h5>
                                        <div class="d-flex gap-3 mb-3">
                                            <button class="btn btn-primary btn-sm px-3" type="button"
                                                onclick="generateReferralLink()">
                                                <i class="fa-solid fa-wand-magic-sparkles me-2"></i>Generate Link
                                            </button>
                                            <button class="btn btn-primary btn-sm px-3" type="button" id="copyButton"
                                                onclick="copyReferralLink()" disabled>
                                                <i class="fa-regular fa-copy me-2"></i>Copy Link
                                            </button>
                                        </div>
                                        <div class="input-group">
                                            <input type="text" class="form-control" id="referralLink"
                                                placeholder="Click 'Generate Link' to create your referral link"
                                                readonly>
                                        </div>
                                    </div>
                                </div>
                            </div>



                        </div>
                    </div>
                </div>
        </section>







        <div class="modal fade" id="cancelOrderModal" tabindex="-1" aria-labelledby="cancelOrderModalLabel"
            aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="cancelOrderModalLabel">Cancel Order</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form id="cancelOrderForm">
                            <input type="hidden" id="orderIdToCancel">
                            <div class="mb-3">
                                <label for="cancelReason" class="form-label">Please provide a reason for
                                    cancellation</label>
                                <textarea class="form-control reason-textarea" id="cancelReason" rows="4" required
                                    minlength="10" maxlength="200"
                                    placeholder="Enter your reason for cancellation (minimum 10 characters)"></textarea>
                                <div class="error-feedback" id="reasonError">
                                    Please provide a detailed reason (minimum 10 characters)
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-danger" id="confirmCancelBtn">Cancel Order</button>
                    </div>
                </div>
            </div>
        </div>


        <div class="modal fade" id="returnOrderModal" tabindex="-1" aria-labelledby="returnOrderModalLabel"
            aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="returnOrderModalLabel">Return Order</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form id="returnOrderForm" novalidate>
                            <input type="hidden" id="orderIdToReturn">
                            <div class="mb-3">
                                <label for="returnReason" class="form-label">Please provide a reason for return</label>
                                <textarea class="form-control" id="returnReason" rows="4" required minlength="10"
                                    maxlength="200"
                                    placeholder="Enter your reason for return (minimum 10 characters)"></textarea>
                                <div class="invalid-feedback">
                                    Please provide a detailed reason (minimum 10 characters)
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary" id="confirmReturnBtn">Submit Return
                            request</button>
                    </div>
                </div>
            </div>
        </div>





        <%- include('../partials/user/footer') %>

            <!-- Your existing scripts -->
            <script src="/assets/js/vendor/modernizr-3.5.0.min.js"></script>
            <script src="/assets/js/vendor/jquery-1.12.4.min.js"></script>
            <script src="/assets/js/popper.min.js"></script>
            <script src="/assets/js/bootstrap.min.js"></script>
            <script src="/assets/js/jquery.slicknav.min.js"></script>
            <script src="/assets/js/owl.carousel.min.js"></script>
            <script src="/assets/js/slick.min.js"></script>
            <script src="/assets/js/wow.min.js"></script>
            <script src="/assets/js/animated.headline.js"></script>
            <script src="/assets/js/jquery.magnific-popup.js"></script>
            <script src="/assets/js/jquery.nice-select.min.js"></script>
            <script src="/assets/js/jquery.sticky.js"></script>
            <script src="/assets/js/main.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
            <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            <script>
                document.addEventListener('DOMContentLoaded', function () {
                    const navLinks = document.querySelectorAll('.nav-link');
                    const sections = document.querySelectorAll('.section');

                    navLinks.forEach(link => {
                        link.addEventListener('click', function (e) {
                            navLinks.forEach(l => l.classList.remove('active'));
                            sections.forEach(s => s.classList.remove('active'));

                            this.classList.add('active');

                            const sectionId = this.getAttribute('data-section');
                            document.getElementById(sectionId).classList.add('active');
                        });
                    });
                });
            </script>
            <script>
                document.addEventListener("DOMContentLoaded", function () {
                    const form = document.getElementById("profileForm");
                    const firstName = document.getElementById("firstName");
                    const phone = document.getElementById("phone");
                    const firstNameError = document.getElementById("firstNameError");
                    const phoneError = document.getElementById("phoneError");

                    form.addEventListener("submit", async function (event) {
                        event.preventDefault();

                        let isValid = true;

                        firstNameError.textContent = "";
                        phoneError.textContent = "";

                        if (firstName.value.trim() === "") {
                            firstNameError.textContent = "First Name is required.";
                            isValid = false;
                        }

                        const phoneRegex = /^[0-9]{10}$/;
                        if (!phoneRegex.test(phone.value.trim())) {
                            phoneError.textContent = "Phone number must be a valid 10-digit number.";
                            isValid = false;
                        }

                        if (isValid) {
                            const formData = {
                                name: firstName.value.trim(),
                                phone: phone.value.trim(),
                            };

                            try {
                                const response = await fetch("/update-profile", {
                                    method: "POST",
                                    headers: {
                                        "Content-Type": "application/json",
                                    },
                                    body: JSON.stringify(formData),
                                });

                                const result = await response.json();

                                if (response.ok && result.success) {
                                    Swal.fire({
                                        title: "Success!",
                                        text: "Profile updated successfully!",
                                        icon: "success",
                                        confirmButtonText: "OK",
                                    }).then(() => {
                                        window.location.href = "/update-profile";
                                    });
                                } else {
                                    Swal.fire({
                                        title: "Failed!",
                                        text: result.message || "Failed to update profile. Please try again.",
                                        icon: "error",
                                        confirmButtonText: "OK",
                                    });
                                }
                            } catch (error) {
                                console.error("Error:", error);
                                Swal.fire({
                                    title: "Error!",
                                    text: "An error occurred while submitting the form. Please try again.",
                                    icon: "error",
                                    confirmButtonText: "OK",
                                });
                            }


                        }
                    });
                });
            </script>

            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    // Initial check for stored active section
                    const storedSection = localStorage.getItem('activeSection');
                    if (storedSection) {
                        // Remove active class from all nav links
                        document.querySelectorAll('.nav-link').forEach(link => {
                            link.classList.remove('active');
                        });
                        // Add active class to stored section
                        const activeLink = document.querySelector(`.nav-link[data-section="${storedSection}"]`);
                        if (activeLink) {
                            activeLink.classList.add('active');
                        }
                        // Clear the stored section
                        localStorage.removeItem('activeSection');
                    }

                    const deleteButtons = document.querySelectorAll('.delete-btn');

                    deleteButtons.forEach((button) => {
                        button.addEventListener('click', (event) => {
                            event.preventDefault();
                            const addressId = button.getAttribute('data-id');

                            Swal.fire({
                                title: 'Are you sure?',
                                text: "You won't be able to revert this!",
                                icon: 'warning',
                                showCancelButton: true,
                                confirmButtonColor: '#3085d6',
                                cancelButtonColor: '#d33',
                                confirmButtonText: 'Yes, delete it!'
                            }).then((result) => {
                                if (result.isConfirmed) {
                                    fetch(`/delete-address/${addressId}`, {
                                        method: 'DELETE',
                                    })
                                        .then((response) => {
                                            if (response.ok) {
                                                // Store the active section before reload
                                                localStorage.setItem('activeSection', 'addresses');

                                                Swal.fire(
                                                    'Deleted!',
                                                    'The address has been deleted.',
                                                    'success'
                                                ).then(() => {
                                                    window.location.reload();
                                                });
                                            } else {
                                                Swal.fire(
                                                    'Error!',
                                                    'There was a problem deleting the address.',
                                                    'error'
                                                );
                                            }
                                        })
                                        .catch(() => {
                                            Swal.fire(
                                                'Error!',
                                                'Unable to connect to the server.',
                                                'error'
                                            );
                                        });
                                }
                            });
                        });
                    });
                });
            </script>


            <script>
                document.getElementById('passwordForm').addEventListener('submit', async function (e) {

                    e.preventDefault();
                    const currentPassword = document.getElementById('currentPassword').value.trim();
                    const newPassword = document.getElementById('newPassword').value.trim();
                    const confirmNewPassword = document.getElementById('confirmNewPassword').value.trim();

                    const currentPasswordError = document.getElementById('currentPasswordError');
                    const newPasswordError = document.getElementById('newPasswordError');
                    const confirmNewPasswordError = document.getElementById('confirmNewPasswordError');

                    currentPasswordError.textContent = '';
                    newPasswordError.textContent = '';
                    confirmNewPasswordError.textContent = '';

                    let isValid = true;

                    if (!currentPassword) {
                        currentPasswordError.textContent = 'Current password is required.';
                        isValid = false;
                    } else if (currentPassword.length < 6) {
                        currentPasswordError.textContent = 'current password must be at least 6 characters long.';
                        isValid = false;
                    }

                    if (!newPassword) {
                        newPasswordError.textContent = 'New password is required.';
                        isValid = false;
                    } else if (newPassword.length < 6) {
                        newPasswordError.textContent = 'New password must be at least 6 characters long.';
                        isValid = false;
                    }

                    if (!confirmNewPassword) {
                        confirmNewPasswordError.textContent = 'Please confirm your new password.';
                        isValid = false;
                    } else if (newPassword !== confirmNewPassword) {
                        confirmNewPasswordError.textContent = 'Passwords do not match.';
                        isValid = false;
                    }

                    if (isValid) {
                        try {
                            const response = await fetch("/update-password", {
                                method: "POST",
                                headers: {
                                    "Content-Type": "application/json",
                                },
                                body: JSON.stringify({ currentPassword, newPassword }),
                            });

                            const result = await response.json();

                            if (response.ok && result.success) {

                                localStorage.setItem('activeSection', 'password');

                                Swal.fire({
                                    title: "Success!",
                                    text: "Password updated successfully!",
                                    icon: "success",
                                    confirmButtonText: "OK",
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else {
                                Swal.fire({
                                    title: "Failed!",
                                    text: result.message || "Failed to update password. Please try again.",
                                    icon: "error",
                                    confirmButtonText: "OK",
                                });
                            }
                        } catch (error) {
                            console.error("Error:", error);
                            Swal.fire({
                                title: "Error!",
                                text: "An error occurred while submitting the form. Please try again.",
                                icon: "error",
                                confirmButtonText: "OK",
                            });
                        }
                    }
                });

                document.addEventListener('DOMContentLoaded', () => {
                    const storedSection = localStorage.getItem('activeSection');
                    if (storedSection) {
                        // Remove active class from all nav links
                        document.querySelectorAll('.nav-link').forEach(link => {
                            link.classList.remove('active');
                        });
                        // Add active class to stored section
                        const activeLink = document.querySelector(`.nav-link[data-section="${storedSection}"]`);
                        if (activeLink) {
                            activeLink.classList.add('active');
                        }
                        // Clear the stored section
                        localStorage.removeItem('activeSection');
                    }
                });
            </script>
            <script>
                app.locals.getStatusColor = function (status) {
                    const statusColors = {
                        'Pending': 'warning',
                        'Confirmed': 'info',
                        'Shipped': 'primary',
                        'Delivered': 'success',
                        'Canceled': 'danger',
                        'Returned': 'secondary'
                    };
                    return statusColors[status] || 'secondary';
                };

                app.locals.getPaymentStatusColor = function (status) {
                    const statusColors = {
                        'Pending': 'warning',
                        'Paid': 'success',
                        'Failed': 'danger',
                        'Canceled': 'danger',
                        'Refunded': 'info'
                    };
                    return statusColors[status] || 'secondary';
                };
            </script>

            <script>
                let cancelOrderModal;
                document.addEventListener('DOMContentLoaded', function () {
                    // Check for stored active section
                    const storedSection = localStorage.getItem('activeSection');
                    if (storedSection) {
                        document.querySelectorAll('.nav-link').forEach(link => {
                            link.classList.remove('active');
                        });
                        const activeLink = document.querySelector(`.nav-link[data-section="${storedSection}"]`);
                        if (activeLink) {
                            activeLink.classList.add('active');
                        }
                        localStorage.removeItem('activeSection');
                    }

                    cancelOrderModal = new bootstrap.Modal(document.getElementById('cancelOrderModal'));

                    document.getElementById('confirmCancelBtn').addEventListener('click', handleCancelOrder);

                    document.getElementById('cancelOrderModal').addEventListener('hidden.bs.modal', function () {
                        document.getElementById('cancelOrderForm').reset();
                        document.getElementById('reasonError').style.display = 'none';
                        document.getElementById('cancelReason').classList.remove('is-invalid');
                    });
                });

                function cancelOrder(orderId) {
                    document.getElementById('orderIdToCancel').value = orderId;
                    cancelOrderModal.show();
                }

                function handleCancelOrder() {
                    const orderId = document.getElementById('orderIdToCancel').value;
                    const reason = document.getElementById('cancelReason').value.trim();
                    const reasonInput = document.getElementById('cancelReason');
                    const reasonError = document.getElementById('reasonError');

                    if (!reason || reason.length < 10) {
                        reasonInput.classList.add('is-invalid');
                        reasonError.style.display = 'block';
                        return;
                    }

                    reasonInput.classList.remove('is-invalid');
                    reasonError.style.display = 'none';

                    const confirmBtn = document.getElementById('confirmCancelBtn');
                    const originalText = confirmBtn.innerHTML;
                    confirmBtn.disabled = true;
                    confirmBtn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Processing...';

                    fetch(`/update-profile/cancel/${orderId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({ cancelReason: reason })
                    })
                        .then(response => response.json())
                        .then(data => {
                            cancelOrderModal.hide();

                            if (data.success) {
                                // Store the active section before reload
                                localStorage.setItem('activeSection', 'orders');

                                Swal.fire({
                                    title: 'Success!',
                                    text: 'Your order has been successfully cancelled.',
                                    icon: 'success',
                                    confirmButtonText: 'OK'
                                }).then((result) => {
                                    if (result.isConfirmed) {
                                        window.location.reload();
                                    }
                                });
                            } else {
                                throw new Error(data.message || 'Failed to cancel order');
                            }
                        })
                        .catch(error => {
                            console.error('Error:', error);
                            cancelOrderModal.hide();

                            Swal.fire({
                                title: 'Error!',
                                text: error.message || 'An error occurred while canceling the order.',
                                icon: 'error',
                                confirmButtonText: 'OK'
                            });
                        })
                        .finally(() => {
                            confirmBtn.disabled = false;
                            confirmBtn.innerHTML = originalText;
                        });
                }
            </script>


            <script>
                let returnOrderModal;
                document.addEventListener('DOMContentLoaded', function () {
                    // Initialize the return order modal
                    returnOrderModal = new bootstrap.Modal(document.getElementById('returnOrderModal'));

                    // Add event listener to the confirm return button
                    document.getElementById('confirmReturnBtn').addEventListener('click', handleReturnOrder);

                    // Reset the form and error state when the modal is hidden
                    document.getElementById('returnOrderModal').addEventListener('hidden.bs.modal', function () {
                        document.getElementById('returnOrderForm').reset();
                        document.getElementById('reasonError').style.display = 'none';
                        document.getElementById('returnReason').classList.remove('is-invalid');
                    });
                });

                // Function to open the return order modal and set the order ID
                function returnOrder(orderId) {
                    document.getElementById('orderIdToReturn').value = orderId;
                    returnOrderModal.show();
                }

                // Handle the return order submission
                function handleReturnOrder() {
                    const orderId = document.getElementById('orderIdToReturn').value;
                    const reason = document.getElementById('returnReason').value.trim();
                    const reasonInput = document.getElementById('returnReason');
                    const reasonError = document.getElementById('reasonError');

                    // Validate the reason
                    if (!reason || reason.length < 10) {
                        reasonInput.classList.add('is-invalid');
                        reasonError.style.display = 'block';
                        return;
                    }

                    reasonInput.classList.remove('is-invalid');
                    reasonError.style.display = 'none';

                    const confirmBtn = document.getElementById('confirmReturnBtn');
                    const originalText = confirmBtn.innerHTML;
                    confirmBtn.disabled = true;
                    confirmBtn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Processing...';

                    // Make the API call to handle the return request
                    fetch(`/update-profile/return/${orderId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({ returnReason: reason })
                    })
                        .then(response => response.json())
                        .then(data => {
                            returnOrderModal.hide();

                            if (data.success) {
                                Swal.fire({
                                    title: 'Success!',
                                    text: 'Your return request has been successfully submitted.',
                                    icon: 'success',
                                    confirmButtonText: 'OK'
                                }).then((result) => {
                                    if (result.isConfirmed) {
                                        window.location.reload();
                                    }
                                });
                            } else {
                                throw new Error(data.message || 'Failed to process return order');
                            }
                        })
                        .catch(error => {
                            console.error('Error:', error);
                            returnOrderModal.hide();

                            Swal.fire({
                                title: 'Error!',
                                text: error.message || 'An error occurred while processing your return request.',
                                icon: 'error',
                                confirmButtonText: 'OK'
                            });
                        })
                        .finally(() => {
                            confirmBtn.disabled = false;
                            confirmBtn.innerHTML = originalText;
                        });
                }
            </script>
            <script>
                function downloadPDF(orderId) {
                    window.location.href = `/download-invoice/${orderId}`;
                }
            </script>

            <script>
                function generateReferralLink() {
                    const referralCode = '<%= user.referralCode %>';
                    const referralLink = `http://localhost:5000/signup?ref=${referralCode}`;

                    const linkInput = document.getElementById('referralLink');
                    linkInput.value = referralLink;

                    document.getElementById('copyButton').disabled = false;

                    Swal.fire({
                        icon: 'success',
                        title: 'Referral Link Generated!',
                        text: 'Your referral link is ready to be copied and shared.',
                        timer: 2000,
                        showConfirmButton: false
                    });
                }

                async function copyReferralLink() {
                    const linkInput = document.getElementById('referralLink');

                    try {
                        await navigator.clipboard.writeText(linkInput.value);

                        Swal.fire({
                            icon: 'success',
                            title: 'Referral Link Generated!',
                            text: 'Your referral link has been copied to clipboard.',
                            timer: 2000,
                            showConfirmButton: false
                        });
                    } catch (err) {
                        Swal.fire({
                            icon: 'error',
                            title: 'Oops...',
                            text: 'Failed to copy link to clipboard'
                        });
                        console.error('Failed to copy text: ', err);
                    }
                }
            </script>
            <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
            <script>
                function retrypayment(orderId) {
                    fetch(`/retry-payment/${orderId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.success) {
                                // Open Razorpay payment modal
                                const options = {
                                    key: data.razorpayKey,
                                    amount: data.amount,
                                    currency: data.currency,
                                    order_id: data.orderId,
                                    name: "watch shop",
                                    description: "Payment for Order",
                                    handler: function (response) {
                                        // Handle successful payment
                                        fetch(`/payment-success/${orderId}`, {
                                            method: 'POST',
                                            headers: {
                                                'Content-Type': 'application/json',
                                            },
                                            body: JSON.stringify(response)
                                        })
                                            .then(response => response.json())
                                            .then(data => {
                                                if (data.success) {
                                                    Swal.fire({
                                                        title: "Payment Successful!",
                                                        text: "Thank you for your payment.",
                                                        icon: "success",
                                                        confirmButtonText: "OK",
                                                    }).then(() => {
                                                        window.location.reload();
                                                    });
                                                } else {
                                                    Swal.fire({
                                                        title: "Payment Failed!",
                                                        text: "Something went wrong. Please try again.",
                                                        icon: "error",
                                                        confirmButtonText: "Retry",
                                                    });
                                                }

                                            });
                                    },

                                    theme: {
                                        color: "#F37254"
                                    }
                                };
                                const rzp = new Razorpay(options);
                                rzp.open();
                            } else {
                                Swal.fire({
                                    title: "Payment Initiation Failed!",
                                    text: "Please check your details and try again.",
                                    icon: "error",
                                    confirmButtonText: "OK",
                                });
                            }
                        });
                }
            </script>


</body>

</html>